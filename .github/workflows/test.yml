name: Test

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref_name }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: "test (${{ matrix.go-version || matrix.go-version-file }})"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['', 'stable', 'oldstable', '1.26.0']
        include:
          - go-version: ''
            go-version-file: 'go.mod'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ matrix.go-version }}
          go-version-file: ${{ matrix.go-version-file }}

      - run: go test -count=1 -race -shuffle=on -coverprofile=coverage.out -covermode=atomic ./...
        if: ${{ matrix.go-version-file == 'go.mod' }}
      - run: go test -count=1 -race -shuffle=on ./...
        if: ${{ matrix.go-version-file != 'go.mod' }}

      - run: go test -count=1 -shuffle=on -run "_Racing$" ./...

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: ${{ matrix.go-version-file == 'go.mod' }}
        with:
          name: coverage
          path: coverage.out
          if-no-files-found: error

  codecov:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: coverage

      - uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7.1.1
        with:
          use_oidc: true
          fail_ci_if_error: true
          disable_search: true
          files: coverage.out
